# フェーズ 7：記事の検索

## 概要

キーワードで記事のタイトル・本文を全文検索する

## 受け入れ条件

- `search <keyword>` コマンドでタイトル・本文に対する部分一致検索ができる
- 検索結果は公開日時の降順で表示する
- 検索結果の表示形式は `list` コマンドと同一（ID・タイトル・公開日時・既読・お気に入り）
- 一致件数が 0 件の場合は「該当記事が見つかりませんでした」と表示して終了コード 0 で終了する
- `--bookmarked` フラグでお気に入り記事のみを検索対象に絞り込める
